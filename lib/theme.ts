import {createTheme} from "@mui/material/styles";

export const mainTheme = createTheme({
  palette: {
    primary: {
      main: "#FA812F",
    },
    secondary: {
      main: "#FAB12F",
    },
  },
});

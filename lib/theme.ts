import {createTheme} from "@mui/material/styles";

export const mainTheme = createTheme({
  palette: {
    primary: {
      main: "#426B1F",
    },
    secondary: {
      main: "#ffffff",
    },
  },
});
